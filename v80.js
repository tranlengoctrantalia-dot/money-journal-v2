(()=>{const KEY='money_journal_v2_state',THEME='mj_theme_mode';const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));const fmt=n=>(+n||0).toLocaleString('vi-VN');const norm=x=>x==='money_in'?'moneyin':x==='debt_payment'?'debt':x;const TYPES=[['spending','Chi tiêu'],['income','Thu nhập'],['moneyin','Tiền nhận'],['debt','Trả thẻ'],['refund','Hoàn tiền'],['transfer','Chuyển tiền']];const CATS=['Ăn uống','Di chuyển','Mua sắm','Hẹn hò','Giải trí','Hóa đơn','Sức khỏe','Làm đẹp','Dịch vụ số','Quà tặng','Phí','Trả góp','Thanh toán dư nợ','Tiết kiệm','Lương','Khác'];const SOURCES=['Tiền mặt','VPBank','Shinhan','Ví Trả Sau','Tài khoản ngân hàng','Tiết kiệm','Khác'];let page='home',month=null,filter='all',q='',editing=null;function load(){try{const s=JSON.parse(localStorage.getItem(KEY)||'{}');s.transactions=Array.isArray(s.transactions)?s.transactions:[];s.accounts=Array.isArray(s.accounts)?s.accounts:[];s.budgets=Array.isArray(s.budgets)?s.budgets:[];return s}catch{return{transactions:[],accounts:[],budgets:[]}}}function save(s){localStorage.setItem(KEY,JSON.stringify(s))}function months(){return[...new Set(load().transactions.map(t=>String(t.date||'').slice(0,7)).filter(x=>/^\d{4}-\d{2}$/.test(x)))].sort().reverse()}function activeMonth(){const ms=months();return month&&ms.includes(month)?month:(ms[0]||new Date().toISOString().slice(0,7))}function ml(ym){const[y,m]=ym.split('-');return`Tháng ${+m}/${y}`}function calc(){const s=load(),ym=activeMonth(),rows=s.transactions.filter(t=>String(t.date||'').startsWith(ym)),sum=t=>rows.filter(x=>norm(x.type)===t).reduce((a,b)=>a+(+b.amount||0),0),income=sum('income'),moneyin=sum('moneyin'),spend=sum('spending'),debt=sum('debt'),refund=sum('refund'),left=income+moneyin-spend-debt+refund,getBal=n=>s.accounts.find(a=>String(a.name||'').toLowerCase()===n.toLowerCase())?.balance||0;return{s,ym,rows,income,moneyin,spend,debt,refund,left,cash:getBal('Tiền mặt'),savings:getBal('Tiết kiệm')}}function txname(t){return t.note||t.name||'Giao dịch'}function txList(list,limit){const a=[...list].sort((x,y)=>String(y.date||'').localeCompare(String(x.date||'')));return(limit?a.slice(0,limit):a).map(t=>`<button class="tx" data-edit="${esc(t.id)}"><time>${String(t.date||'').split('-').reverse().slice(0,2).join('/')}</time><span><b>${esc(txname(t))}</b><small>${esc(t.category||'Khác')}${t.source||t.account?' · '+esc(t.source||t.account):''}</small></span><span class="amt">${fmt(t.amount)}đ</span></button>`).join('')||'<div class="empty">Chưa có giao dịch.</div>'}function catStats(rows){const m=new Map();rows.filter(t=>norm(t.type)==='spending').forEach(t=>m.set(t.category||'Khác',(m.get(t.category||'Khác')||0)+(+t.amount||0)));const r=[...m].map(([category,amount])=>({category,amount})).sort((a,b)=>b.amount-a.amount);return{r,total:r.reduce((a,b)=>a+b.amount,0),max:r[0]?.amount||1}}function home(){const c=calc();return`<section class="cover"><div class="cover-kicker">${ml(c.ym)} · Sổ tiền mùa trăng</div><h1>${fmt(c.left)}đ</h1><p>Phần tiền còn lại sau khi đã chi và trả thẻ.</p><div class="money-ribbon"><div><small>Thu nhập</small><b>${fmt(c.income)}đ</b></div><div><small>Chi tiêu</small><b>${fmt(c.spend)}đ</b></div><div><small>Trả thẻ</small><b>${fmt(c.debt)}đ</b></div></div></section><div class="spread-title"><h2>Tài sản nhỏ</h2><button data-page="settings">Chỉnh số dư</button></div><div class="folio"><div class="folio-card dark"><small>Tiền mặt</small><b>${fmt(c.cash)}đ</b></div><div class="folio-card"><small>Tiết kiệm</small><b>${fmt(c.savings)}đ</b></div></div><div class="spread-title"><h2>Nhật ký gần đây</h2><button data-page="flow">Xem sổ</button></div><div class="timeline">${txList(c.s.transactions,6)}</div>`}function flow(){const c=calc(),ms=months(),idx=Math.max(0,ms.indexOf(c.ym)),st=catStats(c.rows);let list=c.rows;if(filter!=='all')list=list.filter(t=>norm(t.type)===filter);if(q)list=list.filter(t=>`${txname(t)} ${t.category||''} ${t.source||t.account||''}`.toLowerCase().includes(q.toLowerCase()));const out=c.spend+c.debt,pct=out?Math.round(c.spend/out*100):0;return`<div class="page-head"><div><small>Sổ dòng tiền</small><h1>${ml(c.ym)}</h1></div><div class="month-switch"><button data-month="${ms[idx+1]||''}" ${ms[idx+1]?'':'disabled'}>‹</button><button data-month="${ms[idx-1]||''}" ${ms[idx-1]?'':'disabled'}>›</button></div></div><section class="orbit" style="--out:${pct}%"><div class="orbit-ring"><div class="orbit-core"><small>Còn lại</small><b>${fmt(c.left)}đ</b></div></div><div class="orbit-legend"><span>Chi tiêu ${fmt(c.spend)}đ</span><span>Trả thẻ ${fmt(c.debt)}đ</span></div></section><div class="spread-title"><h2>Tiền đi đâu</h2></div><div class="ledger">${st.r.length?st.r.slice(0,8).map(x=>`<div class="ledger-row"><div><b>${esc(x.category)}</b><div class="meter"><i style="width:${Math.max(6,Math.round(x.amount/st.max*100))}%"></i></div></div><span>${fmt(x.amount)}đ</span></div>`).join(''):'<div class="empty">Chưa có chi tiêu.</div>'}</div><div class="spread-title"><h2>Giao dịch</h2></div><div class="seg">${[['all','Tất cả'],['spending','Chi tiêu'],['debt','Trả thẻ'],['income','Thu nhập'],['moneyin','Tiền nhận']].map(([k,v])=>`<button class="${filter===k?'active':''}" data-filter="${k}">${v}</button>`).join('')}</div><input id="search" class="search" value="${esc(q)}" placeholder="Tìm trong sổ"><div class="timeline">${txList(list)}</div>`}function plan(){const c=calc(),b=c.s.budgets||[],map=new Map();c.rows.filter(t=>norm(t.type)==='spending').forEach(t=>map.set(t.category||'Khác',(map.get(t.category||'Khác')||0)+(+t.amount||0)));const limit=b.reduce((a,x)=>a+(+x.limit||0),0),remain=limit-c.spend,used=limit?Math.min(100,Math.round(c.spend/limit*100)):0,g=c.s.goal||{name:'Mục tiêu tiết kiệm',target:0,saved:c.savings},gp=g.target?Math.min(100,Math.round((+g.saved||0)/(+g.target||1)*100)):0;return`<div class="page-head"><div><small>Kế hoạch</small><h1>${ml(c.ym)}</h1></div></div><section class="ticket"><small>CÓ THỂ CHI THÊM</small><h1>${fmt(remain)}đ</h1><div class="ticket-rule"></div><div class="ticket-meta"><span>Đã dùng ${used}% ngân sách</span><span>${fmt(c.spend)}đ / ${fmt(limit)}đ</span></div></section><div class="spread-title"><h2>Phong bì tháng</h2></div><div class="ledger">${b.length?b.map(x=>{const u=map.get(x.name)||0,p=x.limit?Math.min(100,Math.round(u/x.limit*100)):0;return`<div class="ledger-row"><div><b>${esc(x.name)}</b><div class="meter"><i style="width:${p}%"></i></div></div><span>${fmt(u)}đ</span></div>`}).join(''):'<div class="empty">Chưa có ngân sách.</div>'}</div><div class="spread-title"><h2>Mục tiêu</h2></div><div class="folio-card dark"><small>${esc(g.name||'Mục tiêu tiết kiệm')}</small><b>${fmt(g.saved||c.savings)}đ</b><div class="meter" style="margin-top:16px"><i style="width:${gp}%"></i></div></div>`}function settings(){const c=calc(),dark=localStorage.getItem(THEME)==='dark';return`<div class="page-head"><div><small>Kho lưu trữ</small><h1>Cài đặt</h1></div></div><div class="settings-sheet"><div class="setting"><div><b>Giao diện</b><small>${dark?'Tối':'Sáng'}</small></div><button id="themeInline">Đổi</button></div><div class="setting"><div><b>Số dư</b><small>Tiền mặt ${fmt(c.cash)}đ · Tiết kiệm ${fmt(c.savings)}đ</small></div><button id="balances">Chỉnh</button></div></div><div class="spread-title"><h2>Dữ liệu</h2></div><div class="settings-sheet"><div class="setting"><div><b>Sao lưu</b><small>Lưu một bản JSON</small></div><button id="exportBtn">Xuất</button></div><div class="setting"><div><b>Khôi phục</b><small>Đọc file backup</small></div><button id="importBtn">Nhập</button></div><div class="setting"><div><b>Xóa dữ liệu</b><small>Không thể hoàn tác</small></div><button class="danger" id="resetBtn">Xóa</button></div></div><input id="importFile" hidden type="file" accept="application/json">`}function render(){const root=$('#screen');root.innerHTML=page==='home'?home():page==='flow'?flow():page==='plan'?plan():settings();bind();$$('.chapter-rail [data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===page))}function bind(){$$('[data-page]').forEach(b=>b.onclick=()=>{page=b.dataset.page;render();scrollTo({top:0,behavior:'smooth'})});$$('[data-edit]').forEach(b=>b.onclick=()=>openTx(b.dataset.edit));$$('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;render()});$$('[data-month]').forEach(b=>b.onclick=()=>{if(b.dataset.month){month=b.dataset.month;render()}});$('#search')?.addEventListener('input',e=>{q=e.target.value;render()});$('#themeInline')?.addEventListener('click',toggleTheme);$('#balances')?.addEventListener('click',openBalances);$('#exportBtn')?.addEventListener('click',exportData);$('#importBtn')?.addEventListener('click',()=>$('#importFile').click());$('#importFile')?.addEventListener('change',e=>importData(e.target.files[0]));$('#resetBtn')?.addEventListener('click',()=>{if(confirm('Xóa toàn bộ dữ liệu?')){localStorage.removeItem(KEY);render()}})}function sheet(html){const o=$('#overlay');o.innerHTML=`<div class="sheet"><div class="handle"></div>${html}</div>`;o.classList.add('show');o.onclick=e=>{if(e.target===o)close()}}function close(){const o=$('#overlay');o.classList.remove('show');o.innerHTML=''}function openAdd(){sheet(`<h2>Thêm vào sổ</h2><p>Chọn một cách.</p><button class="choice" id="manual"><b>Giao dịch mới</b><small>Nhập nhanh và lưu ngay.</small></button><button class="choice" id="statement"><b>Sao kê</b><small>Scanner đang tạm tách khỏi app chính.</small></button>`);$('#manual').onclick=()=>openTx();$('#statement').onclick=()=>alert('Scanner đang tách riêng để giữ app nhẹ.')}function openTx(id){editing=id||null;const t=id?load().transactions.find(x=>String(x.id)===String(id)):null;sheet(`<h2>${t?'Sửa giao dịch':'Giao dịch mới'}</h2><p>${t?'Chỉnh rồi lưu lại.':'Nhập các thông tin cần thiết.'}</p><div class="form"><label>Loại<select id="fType">${TYPES.map(([v,l])=>`<option value="${v}" ${norm(t?.type)===v?'selected':''}>${l}</option>`).join('')}</select></label><label>Số tiền<input id="fAmount" type="number" inputmode="numeric" value="${t?.amount||''}"></label><label>Ngày<input id="fDate" type="date" value="${t?.date||new Date().toISOString().slice(0,10)}"></label><label>Danh mục<select id="fCat">${CATS.map(v=>`<option ${t?.category===v?'selected':''}>${v}</option>`).join('')}</select></label><label class="full">Nguồn<select id="fSource">${SOURCES.map(v=>`<option ${String(t?.source||t?.account||'')===v?'selected':''}>${v}</option>`).join('')}</select></label><label class="full">Ghi chú<input id="fNote" value="${esc(t?.note||t?.name||'')}"></label></div><div class="actions">${t?'<button class="secondary danger" id="deleteTx">Xóa</button>':''}<button class="secondary" id="cancel">Hủy</button><button class="primary" id="saveTx">Lưu</button></div>`);$('#cancel').onclick=close;$('#saveTx').onclick=saveTx;if(t)$('#deleteTx').onclick=()=>{const s=load();s.transactions=s.transactions.filter(x=>String(x.id)!==String(id));save(s);close();render()}}function saveTx(){const amt=+$('#fAmount').value;if(!amt)return alert('Nhập số tiền.');const s=load(),obj={id:editing||`tx_${Date.now()}`,type:$('#fType').value,amount:amt,date:$('#fDate').value,category:$('#fCat').value,source:$('#fSource').value,note:$('#fNote').value};const i=s.transactions.findIndex(x=>String(x.id)===String(editing));if(i>=0)s.transactions[i]=obj;else s.transactions.push(obj);save(s);close();render()}function toggleTheme(){const next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;localStorage.setItem(THEME,next);render()}function openBalances(){const c=calc();sheet(`<h2>Số dư</h2><p>Cập nhật số dư hiện tại.</p><div class="form"><label>Tiền mặt<input id="cashBal" type="number" value="${c.cash}"></label><label>Tiết kiệm<input id="saveBal" type="number" value="${c.savings}"></label></div><div class="actions"><button class="secondary" id="cancelBal">Hủy</button><button class="primary" id="saveBalBtn">Lưu</button></div>`);$('#cancelBal').onclick=close;$('#saveBalBtn').onclick=()=>{const s=load();function setAcc(name,balance){const i=s.accounts.findIndex(a=>String(a.name||'').toLowerCase()===name.toLowerCase());const obj={...(i>=0?s.accounts[i]:{}),name,balance:+balance||0};if(i>=0)s.accounts[i]=obj;else s.accounts.push(obj)}setAcc('Tiền mặt',$('#cashBal').value);setAcc('Tiết kiệm',$('#saveBal').value);save(s);close();render()}}function exportData(){const blob=new Blob([JSON.stringify(load(),null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`money-journal-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}function importData(file){if(!file)return;const r=new FileReader();r.onload=()=>{try{const s=JSON.parse(r.result);localStorage.setItem(KEY,JSON.stringify(s));render();alert('Đã khôi phục dữ liệu.')}catch{alert('File không hợp lệ.')}};r.readAsText(file)}$('#fab').onclick=openAdd;$('#themeBtn').onclick=toggleTheme;$$('.chapter-rail [data-page]').forEach(b=>b.onclick=()=>{page=b.dataset.page;render();scrollTo({top:0,behavior:'smooth'})});render();if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js').catch(()=>{});})();